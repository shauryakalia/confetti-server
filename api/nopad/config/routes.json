{
  "routes": [
    {
      "method": "GET",
      "path": "/nopad",
      "handler": "nopad.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/nopad",
      "handler": "nopad.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/nopad",
      "handler": "nopad.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
